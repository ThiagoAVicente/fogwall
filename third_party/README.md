Vendored single-header libraries.

- `stb_image.h` — https://github.com/nothings/stb (public domain / MIT), used
  by `src/mpris.c` to decode cached album art (JPEG/PNG) from the active
  MPRIS player for tint extraction.
